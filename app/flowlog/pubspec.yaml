name: flowlog
description: Flowlog — personal coffee intelligence hub.
publish_to: none
version: 0.0.1+1
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  file_selector: ^1.0.3
  share_plus: ^13.2.0
  url_launcher: ^6.3.1
  geolocator: ^14.0.2
  flutter_blue_plus: ^2.3.9
  flowlog_core:
  flowlog_sensors:
  flowlog_charts:
  wakelock_plus: ^1.6.1
  path_provider: ^2.1.5
  path: ^1.9.1
  meta: ^1.16.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift: ^2.30.0

flutter:
  uses-material-design: true
  assets:
    - assets/demo_shot.jsonl
    - assets/minimal_shot.json