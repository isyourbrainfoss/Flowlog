name: flowlog
description: Flowlog — personal coffee intelligence hub.
publish_to: none
version: 0.0.1+1
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  flowlog_core:
  flowlog_sensors:
  flowlog_charts:

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true