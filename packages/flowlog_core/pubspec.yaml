name: flowlog_core
description: Flowlog core models, persistence, and shot session logic.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.16.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6