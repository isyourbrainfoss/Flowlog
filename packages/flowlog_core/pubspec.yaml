name: flowlog_core
description: Flowlog core models, persistence, and shot session logic.
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.16.0
  drift: ^2.30.0
  sqlite3: ^3.0.0
  path: ^1.9.1
  pdf: ^3.13.0
  http: ^1.2.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
  drift_dev: ^2.30.0
  build_runner: ^2.4.15

dependency_overrides:
  cli_util: ^0.5.0